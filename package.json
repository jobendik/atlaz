{
  "name": "nocturne-teaser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An interactive visual-novel teaser — turning hand-drawn characters into a living world.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
